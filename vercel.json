{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "ignoreCommand": "echo modelo_final_CNN.h5 > .gitignore && echo DataPersonExperimento/ >> .gitignore && git check-ignore **/*"
}
